[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pylintalker"
dynamic = ["version"]
description = "Python port of the DECtalk speech synthesizer"
readme = "README.md"
license = {text = "Apache-2.0"}
authors = [{name = "OpenVoiceOS", email = "jarbasai@mailfence.com"}]
requires-python = ">=3.10"
keywords = ["tts", "dectalk", "formant-synthesis", "speech-synthesis"]
dependencies = []

[project.optional-dependencies]
test = [
    "pytest>=7.0.0,<9",
    "pytest-timeout>=2.0.0",
]

[project.urls]
Homepage = "https://github.com/TigreGotico/pylintalker"
Repository = "https://github.com/TigreGotico/pylintalker"

[tool.setuptools]
packages = ["pylintalker"]

[tool.setuptools.dynamic]
version = {attr = "pylintalker.version.__version__"}

[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = ["pylintalker/_data.py"]

[tool.ruff.lint]
select = [
    "E4", "E7", "E9",  # pycodestyle: imports, statements, syntax
    "F",               # pyflakes
    "W",               # pycodestyle warnings
    "I",               # import sorting
    "UP",              # pyupgrade
    "B",               # bugbear
]

[tool.ruff.lint.isort]
known-first-party = ["pylintalker"]

[tool.ruff.lint.per-file-ignores]
# The C-oracle scripts bootstrap sys.path before importing the package.
"test/*" = ["E402"]
